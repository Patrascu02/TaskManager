@using Microsoft.AspNetCore.Identity
@using TaskManager.Modules.Users.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@{
    // Luăm userul curent pentru a afișa numele și inițialele
    var user = await UserManager.GetUserAsync(User);
    string displayName = user?.FullName ?? User.Identity?.Name;
    string initials = "OP"; // Default Operative

    if (!string.IsNullOrEmpty(displayName))
    {
        var parts = displayName.Split(' ', StringSplitOptions.RemoveEmptyEntries);
        if (parts.Length >= 2)
        {
            initials = $"{parts[0][0]}{parts[1][0]}".ToUpper();
        }
        else if (parts.Length == 1)
        {
            initials = parts[0].Substring(0, Math.Min(2, parts[0].Length)).ToUpper();
        }
    }
}

<ul class="navbar-nav ms-auto align-items-center">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle nav-user-link" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <div class="nav-avatar-small">@initials</div>
                <span>SALUT, @displayName.ToUpper()!</span>
            </a>

            <ul class="dropdown-menu dropdown-menu-end cyber-dropdown-menu" aria-labelledby="userDropdown">

                <li>
                    <a class="dropdown-item cyber-dropdown-item" asp-area="" asp-controller="Home" asp-action="UserDashboard">
                        <i class="bi bi-speedometer2"></i> DASHBOARD
                    </a>
                </li>

                <li>
                    <a class="dropdown-item cyber-dropdown-item" asp-area="Identity" asp-page="/Account/Manage/Index">
                        <i class="bi bi-person-gear"></i> CONTUL MEU
                    </a>
                </li>

                <li>
                    <a class="dropdown-item cyber-dropdown-item" asp-area="" asp-controller="Gamification" asp-action="Leaderboard">
                        <i class="bi bi-trophy"></i> CLASAMENT
                    </a>
                </li>

                <li><hr class="dropdown-divider" style="border-color: rgba(255,255,255,0.1);"></li>

                <li>
                    <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                        <button type="submit" class="cyber-logout-btn">
                            <i class="bi bi-power"></i> DECONECTARE
                        </button>
                    </form>
                </li>
            </ul>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link nav-btn-outline" asp-area="Identity" asp-page="/Account/Register">ÎNREGISTRARE</a>
        </li>
        <li class="nav-item">
            <a class="nav-link nav-btn-outline" asp-area="Identity" asp-page="/Account/Login">AUTENTIFICARE</a>
        </li>
    }
</ul>