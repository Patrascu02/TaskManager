# TaskManager - Gamified Task Management System

Aplicație de management al sarcinilor dezvoltată în **ASP.NET Core MVC (.NET 8)**, care integrează elemente de gamification (XP, Nivele, Insigne) pentru a motiva utilizatorii. Proiectul utilizează o arhitectură modulară și Entity Framework Core cu SQL Server.

---

## 📅 Status Implementare (Roadmap)

### 🚀 Sprint 1: Mediu Organizat & Arhitectură (Obiectiv 1)
**Focus:** Structura MVC, Baza de date, Configurare inițială.

#### a) Cod organizat conform arhitecturii MVC
- [x] **Inițializarea proiectului:** Configurat pe .NET Core 8.
- [x] **Structura Modulară:** Organizare avansată pe module (`Modules/Users`, `Modules/Tasks`, `Modules/Gamification`) configurată în `Program.cs`.
- [x] **Modele Inițiale:** `ApplicationUser`, `UserTask`, `IdentityRole`.
- [x] **Layout & UI:** Implementat cu Bootstrap (`_Layout.cshtml`, Navbars condiționale pe roluri).

#### b) Bază de date extensibilă
- [x] **SQL Server & EF Core:** `ApplicationDbContext` configurat.
- [x] **Tabele Gamification:** `Levels`, `Badges`, `UserBadges`, `UserXpHistories`.
- [x] **Relații:** Configurate relațiile 1-n și n-n (Users <-> Tasks, Users <-> Badges).

#### c) Sistem documentat
- [x] **Convenții:** Naming conventions C# standard și structură de foldere clară.

---

### 🛡️ Sprint 2: Mediu Secure & Securitate (Obiectiv 2)
**Focus:** Roluri, Criptare, Izolarea datelor.

#### a) Acces pe bază de rol
- [x] **Sistem de Roluri:** Rolurile `Admin`, `Manager`, `User` sunt create automat la pornire (`SeedDataAsync`).
- [x] **Permisiuni:** `[Authorize(Roles = "Admin")]` implementat pe `UsersController`.
- [x] **Panouri Dedicate:** Redirectare automată către `AdminDashboard`, `ManagerDashboard` sau `UserDashboard` la login.

#### b) Securitate Date
- [x] **Criptare Parole:** Utilizare `ASP.NET Core Identity` (PBKDF2 hashing).
- [x] **Protecție CSRF:** Validare Anti-Forgery Token pe formulare.

#### c) Izolarea Datelor
- [ ] **Acces Exclusiv:** Filtrarea task-urilor doar pentru utilizatorul logat (necesită implementare în `TasksController.Index`).
- [ ] **Protecție Endpoint-uri:** Adăugarea atributului `[Authorize]` pe `TasksController` și `GamificationController`.

---

### 🏆 Sprint 3: Sistem de Recompense & Gamification (Obiectiv 3)
**Focus:** XP Dinamic, Nivele, Insigne.

#### a) Acordarea automată de XP
- [x] **Model XP:** Proprietăți `TotalXp` și istoric `UserXpHistory` implementate.
- [x] **Logica Calcul:** Implementată în `TasksController`. Se acordă **50 XP de bază**, ajustați dinamic în funcție de feedback-ul utilizatorului la finalizare (Factor de corecție +/- 10% per punct de diferență față de estimarea inițială).
- [x] **Actualizare DB:** XP-ul se salvează tranzacțional odată cu închiderea task-ului.

#### b) Sistem de Gamificare
- [x] **Level Up:** Calcul automat la fiecare finalizare de task (`TotalXp / 100 + 1`). Utilizatorul primește notificare "🎉 Level Up!" prin TempData.
- [x] **Insigne:** Structura de date pentru `Badge` și `UserBadge` este pregătită.

---

## ⚙️ Instrucțiuni de Instalare și Rulare

1. **Clonare Repository:**
   ```bash
   git clone <repo_url>
