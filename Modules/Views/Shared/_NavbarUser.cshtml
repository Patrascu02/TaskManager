@using Microsoft.AspNetCore.Identity
@using TaskManager.Modules.Users.Models
@inject UserManager<ApplicationUser> UserManager
@{
    var user = await UserManager.GetUserAsync(User);
    string displayName = user?.FullName ?? User.Identity?.Name;
}

<nav class="navbar navbar-expand-lg custom-navbar sticky-top navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="UserDashboard">
            <i class="bi bi-cpu-fill me-2"></i>TASK_MANAGER
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navUser">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navUser">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link font-monospace" asp-controller="Home" asp-action="UserDashboard">
                        <i class="bi bi-speedometer2 text-info"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link font-monospace" asp-controller="Gamification" asp-action="MyProgress">
                        <i class="bi bi-graph-up-arrow text-warning"></i> Progres
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link font-monospace" asp-controller="Gamification" asp-action="Leaderboard">
                        <i class="bi bi-trophy text-danger"></i> Clasament
                    </a>
                </li>
            </ul>

            <ul class="navbar-nav ms-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle cyber-nav-link" href="#" id="uDrop" role="button" data-bs-toggle="dropdown">
                        <span>SALUT, @displayName?.ToUpper()!</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end cyber-dropdown-menu">
                        <li>
                            <a class="dropdown-item cyber-dropdown-item" asp-area="Identity" asp-page="/Account/Manage/Index">
                                <i class="bi bi-person-gear"></i> Profilul Meu
                            </a>
                        </li>
                        <li><hr class="dropdown-divider" style="border-color: rgba(255,255,255,0.1);"></li>
                        <li>
                            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                <button type="submit" class="cyber-logout-btn">
                                    <i class="bi bi-power"></i> DECONECTARE
                                </button>
                            </form>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>